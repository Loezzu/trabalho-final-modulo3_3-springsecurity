PERSONINFO API
- CREATE
- DELETE BY ID
- GET ALL / BY ID
- UPDATE BY ID
- UPDATE MY USER(CRIAR)

ADDRESS API
- CREATE
- DELETE BY ID
- GET ALL / BY ID
- UPDATE BY ID
- UPDATE MY USER(CRIAR)

USER API 
- CREATE
- DELETE BY ID
- GET COMPLETE / BY ID
- UPDATE BY ID
- LIKES DADOS
- LIKES RECEBIDOS
- GET MACTHES
- DELETE MY USER(CRIAR)// FEITO
- UPDATE MY USER(CRIAR)// FEITO
- GET MY USER(CRIAR)// FEITO
- MOSTRAR USUARIOS COMPATIVEIS(CRIAR)

LIKE API *ADICINAR OS USERNAMES NO RETORNO DO MÉTODO
- GET LIKES
- DELETA UM LIKE
- MOSTRA OS LIKES DE UM USER
- DELETA OS LIKES DE UM USUARIO
- DAR LIKE(MUDAR PARAMETROS PARA USAR USUARIO LOGADO)
- DELETAR MEUS LIKES(CRIAR)

MATCH API *ADICINAR OS USERNAMES NO RETORNO DO MÉTODO
